import React from "react";

const Layout = ({children}) => {
    return (
        <div>
            Special layout for hello Page
            {children}
        </div>
    );
};

export default Layout